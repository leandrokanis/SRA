<%= form_for(@tipo_atendimento) do |f| %>
  <% if @tipo_atendimento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_atendimento.errors.count, "error") %> prohibited this tipo_atendimento from being saved:</h2>

      <ul>
      <% @tipo_atendimento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <a href="f.submit"><button class="btn btn-success" type="submit">Concluir</button></a>
    <a href="javascript:window.history.go(-1)"><button class="btn btn-primary" type="button">Cancelar</button></a>

  </div>
<% end %>
