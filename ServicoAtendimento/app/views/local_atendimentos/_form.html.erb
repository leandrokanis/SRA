<%= form_for(@local_atendimento) do |f| %>
  <% if @local_atendimento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@local_atendimento.errors.count, "error") %> prohibited this local_atendimento from being saved:</h2>

      <ul>
      <% @local_atendimento.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
