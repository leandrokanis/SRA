<div class="container">
  <div class="row">
    <div class="page-header">
      <h1>Novo Aluno </h1>
    </div>

    <% if user_signed_in? %>
    <%= render 'form' %>

    <% else %>
	<%= render 'form-main' %>
	<% end %>


  </div>
</div>
