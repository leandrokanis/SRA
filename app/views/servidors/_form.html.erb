<div class="page-header">
<%= simple_form_for(@servidor) do |f| %>
</div>
  <% if @servidor.errors.any? %>
     <div class="alert alert-danger">
      <ul>
      <% @servidor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Nome" %><br />
    <%= f.input :name, label: false %>


    <%= f.label "Matricula" %><br />
    <%= f.input :matricula, label: false %>

    <%= f.label "Email" %><br />
    <%= f.input :email, label: false %>

    <%= f.label "Senha" %><br />
    <%= f.input :password, label: false %>

    <%= f.label "Confirmação de Senha" %><br />
    <%= f.input :password_confirmation, label: false %>

  </div>
    <div class="actions">
  <%=link_to raw("<i class=\"icon-white icon-arrow-left\"> Voltar </i>"), 
          servidors_path , 
          class: "btn  btn-warning"%>
    <%= f.submit "Criar Servidor UnB", :class => 'btn-success btn' %>
    
  </ul>
</div>

<% end %>
