<div class="container">
  <div class="row">
<div class="page-header">
<h1>Lista de Servidores Administrativos</h1>
  <%= render partial: "shared/navbar_collapsible"%>
  
  <div align="right" >
<%=link_to raw("<i class=\"icon-white icon-plus\"> Criar Servidor UnB </i>"), 
          new_servidor_path, 
          class: "btn btn-success  btn-small"%>
</div>
</div>
<table>
  <tr>
    <th>Nome</th>
    <th>Matrícula</th>
    <th></th>
    <th></th>
  </tr>

<% @servidors.each do |servidor| %>
  <tr>
    <td><%= servidor.name %></td>
    <td><%= servidor.matricula %> </td>
    <td><%= link_to raw("<i class=\"fa fa-pencil-square-o\"> Editar </i>"),
          edit_servidor_path(servidor), 
          class: "btn btn-small btn-primary",
          method: :get %></td>
    <td><%= link_to raw("<i class=\"icon-white icon-trash\"> Deletar </i>"), 
          servidor, 
          class: "btn btn-small btn-danger",
          method: :delete, data: { confirm: 'Tem certeza que deseja excluir?'} %></td></td>
  </tr>
<% end %>
</table>

<br />
</div>
</div>
